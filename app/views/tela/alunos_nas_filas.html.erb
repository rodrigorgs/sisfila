<div class="container">
  <div class="links">
    <p><%= link_to 'Consultar matrícula', '/' %></p>
    <p><%= link_to 'Inscrever-se', inscricao_path %></p>
    <p><%= link_to 'Acompanhar telão', '/tela' %></p>
  </div>

  <h1>Filas</h1>
  <div style="display: flex; gap: 5rem; margin-bottom: 3rem; text-align: center">
    <% @filas.each do |fila| %>
      <div>
        <h2><%= fila.codigo %></h2>

        <% if fila.vagas.any? %>
          <table>
            <thead>
            <tr>
              <th>Posição Geral</th>
              <th>Nome</th>
            </tr>
            </thead>
            <tbody>
            <% fila.vagas.each do |vaga| %>
              <% if vaga.posicao > fila.posicao %>
                <tr>
                  <td><%= vaga.posicao - fila.posicao%></td>
                  <td><%= vaga.aluno.nome %></td>
                </tr>
              <% end %>
            <% end %>
            </tbody>
          </table>
        <% else %>
          <p>Não há alunos nesta fila.</p>
        <% end %>
      </div>
    <% end %>
  </div>
</div>


