<p id="notice"><%= notice %></p>

<h1>Vagas</h1>

<table>
  <thead>
    <tr>
      <th>Pos.</th>
      <th>Matrícula</th>
      <th>Nome</th>
      <!-- <th>Data chamada</th> -->
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @vagas.each do |vaga| %>
      <tr>
        <td>
          <% if @posicao_atual == vaga.posicao %>
            <b>**</b>
          <% end %>
          <%= vaga.posicao %>
        </td>
        <td>
          <%= vaga.aluno.matricula %>  
        </td>
        <td><%= vaga.aluno.nome %></td>
        <!-- <td><%= vaga.data_chamada %></td> -->
        <td><%= link_to 'Show', vaga %></td>
        <td><%= link_to 'Edit', edit_vaga_path(vaga) %></td>
        <td><%= link_to 'Destroy', vaga, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Vaga', new_vaga_path %>
